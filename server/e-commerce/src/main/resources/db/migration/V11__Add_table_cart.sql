CREATE TABLE cart
(
    id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    CONSTRAINT pk_cart PRIMARY KEY (id)
);

ALTER TABLE customer
    ADD cart_id BIGINT;

ALTER TABLE customer
    ADD CONSTRAINT uc_customer_cart UNIQUE (cart_id);

ALTER TABLE customer
    ADD CONSTRAINT FK_CUSTOMER_ON_CART FOREIGN KEY (cart_id) REFERENCES cart (id);